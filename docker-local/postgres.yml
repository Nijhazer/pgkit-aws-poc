version: '3.8'

services:
    postgres:
        image: postgres:16
        environment:
            POSTGRES_USER: exampleapp
            POSTGRES_PASSWORD: exampleapp
            POSTGRES_DB: exampleapp
        ports:
        - "5432:5432"
